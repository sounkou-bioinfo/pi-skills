{
  "name": "pi-skills",
  "version": "1.1.1",
  "private": false,
  "description": "Skills-first Pi package with permanent Semantic-SQL memory, bioinformatics and R skills, detachable tasks, system-R RLM, and goals.",
  "license": "MIT",
  "type": "module",
  "keywords": [
    "pi-package",
    "pi-extension",
    "pi-skill",
    "bioinformatics",
    "r",
    "duckdb"
  ],
  "files": [
    "extensions",
    "skills",
    "docs",
    "scripts",
    "README.md",
    "README.Rmd",
    "CHANGELOG.md",
    "LICENSE"
  ],
  "scripts": {
    "postinstall": "node scripts/patch_pi_background_tasks.mjs",
    "typecheck": "tsc --noEmit",
    "test": "npm run test:background-tasks && npm run test:memory && npm run test:rlm && npm run test:anti-slop",
    "test:background-tasks": "rm -rf .background-task-test-build && tsc --target ES2023 --module NodeNext --moduleResolution NodeNext --strict --skipLibCheck --esModuleInterop --types node --rootDir . --outDir .background-task-test-build scripts/test_pi_background_tasks_patch.ts node_modules/pi-background-tasks/src/core/common.ts node_modules/pi-background-tasks/src/core/registry.ts && node .background-task-test-build/scripts/test_pi_background_tasks_patch.js; status=$?; rm -rf .background-task-test-build; exit $status",
    "test:memory": "rm -rf .memory-test-build && tsc --noEmit false --outDir .memory-test-build --declaration false && cp extensions/memory/*.sql .memory-test-build/memory/ && node --test .memory-test-build/memory/memory.test.js; status=$?; rm -rf .memory-test-build; exit $status",
    "test:rlm": "rm -rf .rlm-test-build && tsc --noEmit false --outDir .rlm-test-build --declaration false && node --test .rlm-test-build/rlm/rlm.test.js; status=$?; rm -rf .rlm-test-build; exit $status",
    "test:anti-slop": "Rscript scripts/test_anti_slop.R",
    "render:readme": "Rscript scripts/render_readme.R",
    "check:readme": "Rscript scripts/render_readme.R --check"
  },
  "pi": {
    "extensions": [
      "./extensions",
      "./node_modules/pi-background-tasks/extensions/background-tasks.ts"
    ],
    "skills": [
      "./skills"
    ],
    "prompts": []
  },
  "peerDependencies": {
    "@earendil-works/pi-ai": "*",
    "@earendil-works/pi-coding-agent": "*",
    "typebox": "*"
  },
  "peerDependenciesMeta": {
    "@earendil-works/pi-ai": {
      "optional": true
    },
    "@earendil-works/pi-coding-agent": {
      "optional": true
    },
    "typebox": {
      "optional": true
    }
  },
  "dependencies": {
    "@duckdb/node-api": "^1.5.4-r.1",
    "pi-background-tasks": "0.6.0"
  },
  "bundledDependencies": [
    "pi-background-tasks"
  ],
  "devDependencies": {
    "@earendil-works/pi-ai": "^0.82.1",
    "@earendil-works/pi-coding-agent": "^0.82.1",
    "@types/node": "^24.0.0",
    "typebox": "^1.1.38",
    "typescript": "^5.8.3"
  }
}
